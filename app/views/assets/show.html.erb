<p id="notice"><%= notice %></p>

<p>
  <b>User:</b>  <%= @asset.user_id %>
  
  >
  
  
  
  
  
  
  <b>Click to download</b><%= link_to @asset.file_name, download_url(@asset) %>
</p>


<%= link_to 'Edit', edit_asset_path(@asset) %> |
<%= link_to 'Back', assets_path %>
